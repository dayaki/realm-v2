import React from 'react';
import styled from 'styled-components';

const NoteDetails = () => {
  return <Container></Container>;
};
NoteDetails.navigationOptions = {
  title: 'Note',
};
const Container = styled.ScrollView`
  flex: 1;
`;

export default NoteDetails;
